const {
  EditorAutoSave
} = require('./editorAutoSave.js');
const {
  registerWorkbenchContribution2
} = require('../../../common/contributions.js');
registerWorkbenchContribution2(EditorAutoSave.ID, EditorAutoSave, 2);