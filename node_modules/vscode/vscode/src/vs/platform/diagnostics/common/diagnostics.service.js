const {
  createDecorator
} = require('../../instantiation/common/instantiation.js');
const {
  ID
} = require('./diagnostics.js');
const IDiagnosticsService = createDecorator(ID);
exports.IDiagnosticsService = IDiagnosticsService;