const {
  Action
} = require('../../../common/actions.js');
const {
  localizeWithPath
} = require('../../../../nls.js');
const _moduleId = "vs/base/browser/ui/tree/treeDefaults";
class CollapseAllAction extends Action {
  constructor(viewer, enabled) {
    super('vs.tree.collapse', localizeWithPath(_moduleId, 0, "Collapse All"), 'collapse-all', enabled);
    this.viewer = viewer;
  }
  async run() {
    this.viewer.collapseAll();
    this.viewer.setSelection([]);
    this.viewer.setFocus([]);
  }
}
exports.CollapseAllAction = CollapseAllAction;